package br.com.reportmanager.report.resource;

import java.io.File;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/relatorio")
public class ReportResource {

	@GetMapping
	public ResponseEntity<String> testando() {

		File file = new File(ReportResource.class.getClassLoader().getResource("teste.txt").toString());

		file.mkdirs();

		return ResponseEntity.ok("Deu tudo certo");
	}

}
